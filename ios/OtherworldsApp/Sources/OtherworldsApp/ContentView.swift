import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Otherworlds RPG")
                .font(.largeTitle)
            Text("iOS Client Placeholder")
                .foregroundStyle(.secondary)
        }
        .padding()
    }
}
